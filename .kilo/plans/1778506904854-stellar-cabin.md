# NFC PN532 Post-Fix Validation Plan

## Status
6/6 bugs fixed. Firmware builds. Unit tests have pre-existing JS link errors (unrelated). External NFC app audit complete (~16 broken, listener mode unsupported).

## Done
1. Mutex race: removed acquire/release from 4 HAL wrapper funcs
2. PN532 missing mutex: added acquire/release in exchange_internal + trx_short_frame
3. Scanner filter: PN532 offers A/B/FeliCa (not just A)
4. Listener guard: set_col_res_data wrapped in #ifndef PN532_ONLY
5. furi_check: added to low_power_mode_stop acquire
6. 270-byte read: diagnosed, minor perf only
7. I2C desync: bus reset disabled (no HW RST pin), retries only
8. test_runner.c: cli → pipe fix (pre-existing build error)
9. nfc_mock.c: stub nfc_wait_for_poller_ready for unit tests
10. MF Classic: type detect by ATQA/SAK not auth sectors

## Remaining Work

### Phase 1: Flash + Real Hardware Test
- Flash firmware to DIY board (`./fbt flash_usb_full`)
- Test NFC read with known MIFARE Classic tag
- Test NFC read with NTAG/Ultralight
- Verify I2C no desync over multiple read cycles
- Collect logs for poll success rate, latency

### Phase 2: ISO14443-4A + MF Classic Verify
- test ATS read (RATS exchange through PN532 InDataExchange)
- test MF Classic auth (nested + standard)
- test sector read/write
- return: pass/fail + log excerpts

### Phase 3: ISO14443-3B + FeliCa
- These use poller_tx_common → returns Communication for PN532 (expected)
- Verify scanner cycle handles them cleanly (no crash/hang)
- return: scanner log showing B+FeliCa skip

### Phase 4: Tone Down Debug Logging ✓
- furi_hal_nfc_pn532.c: removed 11 noisy D-level logs (wait_event trace, trx_short_frame polling, set_mode)
- furi_hal_pn532.c: removed TX hex dump, read_ack step trace, exchange + poll_iso14443a D logs (~10 lines)
- furi_hal_nfc_timer.c: removed ISR trace, timer start/stop/skip/init/deinit/FWT start logs (~8 lines)

firmware.bin: 215 pages, free_flash 167.41K (gained 1.75K from log removal)

### Phase 5: Unit Tests Fix ✓
- JS entries removed from `unit_test_api_table_i.h` (js_thread_run/stop, js_value_buffer_size/parse)
- root cause: JS app compiled as FAP plugin, not linked into firmware ELF
- `./fbt FIRMWARE_APP_SET=unit_tests` now passes clean

### Phase 6: PN532 Bypass Circuit (if needed)
- If I2C+daisy-chained ICs cause bus contention → SPDT switch
- sketch schematic: PN532 SDA/SCL → SPDT (VCC sense) → CPU I2C1
- schematic doc not firmware work, skip for now

## Priority
1. Phase 1 (HW test) — gate for all subsequent work
2. Phase 2 (MF Classic/4A) — most used protocol
3. Phase 3 (B/FeliCa scan)
4. Phase 4 (log cleanup)
5. Phase 5 (unit tests) — only if build clean

## Acceptance
- Detects MIFARE Classic, reads UID + sector 0
- Detects NTAG/Ultralight, reads UID + data
- Scanner cycles through A/B/FeliCa without crash
- No I2C desync after 50+ consecutive reads
- `./fbt` passes clean