# Fix: Approve SRIX API entries and bump SDK version

## Problem

5 new SRIX functions in `targets/f7/api_symbols.csv` have status `?` (PENDING/WIP), causing `SdkCache.is_buildable()` (at `scripts/fbt/sdk/cache.py:67`) to return `False`. The build system refuses with "SDK version is not finalized".

## Changes

### File: `targets/f7/api_symbols.csv`

1. **Line 2**: Bump `Version,+,87.5,,` → `Version,+,87.6,,` (minor bump for new API functions)

2. **Lines 1667-1671**: Approve 5 SRIX entries by changing status `?` → `+`:
   - `Function,?,furi_hal_pn532_srix_detect,...` → `Function,+,...`
   - `Function,?,furi_hal_pn532_srix_get_uid,...` → `Function,+,...`
   - `Function,?,furi_hal_pn532_srix_read_block,...` → `Function,+,...`
   - `Function,?,furi_hal_pn532_srix_select,...` → `Function,+,...`
   - `Function,?,furi_hal_pn532_srix_write_block,...` → `Function,+,...`

## After

Re-run the build command (`./fbt` or equivalent). The SDK check will pass because:
- `_have_pending_entries()` returns `False` (all entries are `+`)
- `version_action == VersionBump.NONE` (no new/changed entries detected vs cache on subsequent run)
- `version != SdkVersion(0, 0)` (it's `87.6`)
